##! Please refer to the documentation at
##! https://coreruleset.org/docs/development/regex_assembly/.

##! Matching one '@' will be enough to detect this.

##! We also added whitespace as it will be executed until the terminating `;` by Perl.

@+\{\s*\[[^\]]+\]
